/*  Votre code Javascript vient ici:
      - Vous pourrez visualiser le résultat en ouvrant index.html dans votre navigateur.
      - N'oubliez pas de sauvegarder pour afficher les changements.
*/

alert( alert(1) && alert(2) ); // 1 puis undefined

// affiche 1 mais garde en mémoire undefined
// puis compare l'opérande de droite qui est égal a true
// donc on a false et true, ici false prend le dessus
// et affiche undefined qui était gardé en mémoire